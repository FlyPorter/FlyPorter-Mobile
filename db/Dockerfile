FROM postgres:16-alpine

# Expose the default PostgreSQL port.
EXPOSE 5432

# Place any SQL or shell scripts into docker-entrypoint-initdb.d by adding
# them to the build context. They will run automatically the first time the
# database starts with a fresh volume.

