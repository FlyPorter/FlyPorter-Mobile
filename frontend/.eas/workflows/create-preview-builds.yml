name: Create Preview Builds

on:
  push:
    branches:
      - main
    paths:
      - 'frontend/**'

jobs:
  build_android:
    type: build
    params:
      platform: android
      profile: preview
  build_ios:
    type: build
    params:
      platform: ios
      profile: preview

