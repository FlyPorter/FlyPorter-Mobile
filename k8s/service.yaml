apiVersion: v1
kind: Service
metadata:
  name: flyporter-backend
  namespace: flyporter
  labels:
    app: flyporter-backend
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: flyporter-backend

